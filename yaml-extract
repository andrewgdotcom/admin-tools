#!/bin/bash
SCRIPT_DIR=$(dirname "${BASH_SOURCE[0]}")
. $SCRIPT_DIR/poshlib/poshlib.sh || exit 1
use-from .
use swine
use yaml-elastic

if [[ $1 == "-r" ]]; then
    shift
    yaml-replace "$@"
else
    yaml-extract "$@"
fi
