#!/bin/bash
SCRIPT_DIR=$(dirname "${BASH_SOURCE[0]}")
. $SCRIPT_DIR/poshlib/poshlib.sh || exit 1
use-from .
use swine
use yaml-elastic

if [[ $1 == "-r" ]]; then
    shift
    yaml-replace "$@"
elif [[ $1 == "-d" ]]; then
    shift
    yaml-delete "$@"
else
    yaml-extract "$@"
fi
